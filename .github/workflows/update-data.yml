name: Update Field Reports Data

on:
  # Run every day at 1 PM UTC
  schedule:
    - cron: '0 13 * * *'
  
  # Allow manual trigger from GitHub Actions tab
  workflow_dispatch:
  
  # Optional: Run on push to specific files (for testing)
  push:
    branches:
      - main
    paths:
      - 'fetch_reports.py'
      - 'aggregate_reports.py'

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Use PAT or default token for pushing
          token: ${{ secrets.GITHUB_TOKEN }}
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
          
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas
          
      - name: Fetch field reports from IFRC GO API
        run: |
          echo "📥 Fetching latest field reports..."
          python fetch_reports.py
          
      - name: Aggregate data for visualization
        run: |
          echo "📊 Aggregating data by month and disaster type..."
          python aggregate_reports.py
          
      - name: Check if data has changed
        id: check-changes
        run: |
          if git diff --quiet field_reports.json field_reports_aggregated.json field_reports.csv; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No changes detected in data files"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ Data has been updated"
          fi
          
      - name: Commit and push updated data
        if: steps.check-changes.outputs.changed == 'true'
        run: |
          git config --local user.name 'github-actions[bot]'
          git config --local user.email 'github-actions[bot]@users.noreply.github.com'
          git add field_reports.json field_reports_aggregated.json field_reports.csv
          git commit -m "chore: Update field reports data [automated]
          
          - Updated: $(date -u +'%Y-%m-%d %H:%M:%S UTC')
          - Total reports: $(jq '. | length' field_reports.json)
          - Workflow: ${{ github.workflow }} #${{ github.run_number }}"
          git push
          
      - name: Deployment info
        if: steps.check-changes.outputs.changed == 'true'
        run: |
          echo "🚀 Data updated successfully!"
          echo "📍 Vercel will automatically redeploy with the new data"
          echo "🔗 Check your Vercel dashboard for deployment status"
          
      - name: No changes info
        if: steps.check-changes.outputs.changed == 'false'
        run: |
          echo "ℹ️ No new data to update"
          echo "📅 Last run: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
